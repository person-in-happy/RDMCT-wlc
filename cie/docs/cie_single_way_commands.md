# 历史归档：旧单路命令

> 已停用。该文件原先绑定旧盘符、旧模型与未完成 campaign，且部分文本编码损坏。

当前单路正式流程请使用 [`../README.md`](../README.md) 中的 `run_cie_single.ps1` 命令。不要从 Git 历史恢复旧 seed、时间、内存、ACS latest 或 checkpoint 口径后用于论文。

旧 campaign 如需取证，只能保留其原始 commit、命令、LP、模型和断点，不得与新的 `*_repro_v1` 结果合并。
